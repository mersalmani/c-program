#include<stdio.h>
int main()
{
int a[50],b[50];
int i;
void merge(int low,int mid,int high)
int n;
if(a==b)
{
printf("\nenter the element are:");
scanf("%d",&n);
if(low<high)
{
mid=(low+high)/2;
sort(low,mid);
sort(mid+1,high);
merge(low,mid,high);
else
return;
}}
return 0;

