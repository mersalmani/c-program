#include<stdio.h>
int main()
{
int a,b,c,sum,cube;
sum=a*100+b*10+c*1;
cube=a*a*a+b*b*b+c*c*c;
printf("enter the number;");
scanf("%d%d",&cube,&sum);
if(sum==cube)
{printf("it is armsrtong");
else
printf("it is not armstrong");
}
return 0;
}

