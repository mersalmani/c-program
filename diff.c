#include<stdio.h>
int main()
{
int a,b,c,sum,cube=0,i,n;
sum=a*100+b*10+c*1;
cube=a*a*a+b*b*b+c*c*c;
printf("\n enter the number bw %d and %d is");
scanf("%d%d",&n);
if(i=0;i<n;++i);
if(cube==sum)
{
printf("yes");
else
printf("no");
}
return0;
}
