#include<stdio.h>
int main()
{
int base,exp;
int result=1;
printf("enter the base number:");
scanf("%d",&base);
printf("the exp number:");
scanf("%d',&exp);
while(exp!=0);
result *=base;
--exp;
printf("answer is %d",result);
return 0;
}
